Для базы данных запустить скрипт dbset.
В скрипте используется программа pg_restore, идет в комплекте с postgres.

В параметрах запуска указать имя пользователя, название бд (по желанию)
и файл dump.backup.

По результату должна быть создана указаная бд с нужной таблицей. 
(указано в выводе)
В случае ошибки вывод идет в файл error (ошибки от программы pg_restore).

После работы скрипта получаем данные для ввода в диалоге программы.
(без пароля пользователя и порта (порт обычно 5432).

Папки:
build - сборка без библиотек.
inter - исходные файлы.
prog_deploy - развернутое приложение.

Для запука использовать ссылку start. [запустит приложение с окружением].
(используется CQtDeployer)

Принцип работы программы.
После запуска открываеются 2 окна (окно с графиком и управлением).

Если в меню выбрать пункт БД и указать данные, то получим данные из БД.
(иначе кнопки с БД недоступны)

В программе можно указать время начальной симуляции движения курсов.
(при заданных парамтрах программа просчитает движение)


(в программе используются подсказки при наведении)
Указать параметры в диалогах [1 объект и 2 объект].

K - угол наклона
V - скорость в м/с. (с нулевой скорость объект не движется)
X1 Y1 - начальные позиции объекта.
X2 Y2 - текущие позиции.

При пересечении курсов автоматически устанавливаются
D - расстояние от точки пересечения до текущей позиции.
T - время пересечения.

Принять изменения сбросит текущую симуляцию.
Отмена не произведет изменений.

Нажать кнопку Старт для начала или продолжения симуляции.
Нажать кнопку Стоп для приостановки симуляции.
Нажать кнопку Рестарт для сброса симуляции.
